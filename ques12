#include<stdio.h>
int main()
{
	int n,i,sum=0;
	
	printf("enter a range:");
	scanf("%d",&n);
	
	for(i=i;i<=n;i++)
	{
		if(i%2!=0)
	    	sum+=i*i;
	}
	printf("Sum=%d",sum);
}

OUTPUT:
enter a range:5
Sum=35
